# Vercel이 Python 환경을 구성할 때 이 파일을 읽어서 패키지를 설치합니다.
# urllib은 Python 표준 라이브러리라 여기 적을 필요 없어요.
# 나중에 anthropic(Claude) 등을 쓰고 싶으면 여기에 추가하면 됩니다.

# 현재는 표준 라이브러리만 사용하므로 비워도 되지만,
# Vercel이 Python 프로젝트임을 인식하도록 파일 자체는 존재해야 합니다.